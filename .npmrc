# Faster, quieter installs.
# Skip the audit and funding network round-trips on every install.
audit=false
fund=false
# Reuse the local cache before hitting the network.
prefer-offline=true
# Less console noise during CI / scripted installs.
loglevel=warn
